{% extends 'base.html.twig' %}

{% block main %}
    <h1>Liste des catégories</h1>

    {% for category in categories %}

        <h2>{{ category.title }}</h2>

    {% endfor %}
{% endblock main %}


