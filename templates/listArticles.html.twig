{% extends 'base.html.twig' %}

{% block main %}

    {% if %}

    <h1>Liste des articles</h1>


    {% for article in articles %}

        <h2>{{ article.title }}</h2>
        <a href="{{ path('single_article', {'id' : article.id }) }}">Voir l'article complet</a>

    {% endfor %}
{% endblock main %}


